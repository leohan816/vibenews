# Reviewer Handoff — implementation-review-001

1. Verify the exact repo, origin, `master`, fetched `origin/master`, clean worktree, subject/frozen-design/review/evidence
   objects, ancestry, path counts, and commit ownership.
2. Open `REVIEWER_BRIEF.md`, both root entries, every canonical protocol, all active-job authority artifacts, both
   design-review reports, every frozen-design subject, every implementation subject, and Worker evidence directly.
3. Read the complete base-to-subject diff, initial implementation diff, validation-correction delta, and all relevant
   current code. Do not substitute summaries, commit messages, test names, or prior Reviewer memory.
4. Perform only independent `IMPLEMENTATION_REVIEW`. Run the complete synthetic checks specified in the brief. Do not
   open/source `.env.server.local`, call live YouTube/DeepSeek/Fish, install/start systemd, mutate the runtime database or
   generated-audio store, alter Tailscale, or perform device acceptance.
5. Do not edit, patch, freeze, implement, accept risk for Leo/GPT, route another actor, or launch any agent/subagent.
6. Write only:
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-review-001/REVIEW_RESULT.md`
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-review-001/REVIEW_RESULT_POINTER.md`
7. Use every canonical Reviewer result field. Target exactly `SUBJECT_HEAD` and the 86 declared implementation paths;
   record the frozen design and both design-review heads; keep Worker evidence outside the verdict target. Use
   `REPORT_HEAD: RECORDED_AFTER_REPORT_PUSH_IN_CHAT` in both containing files.
8. For `NEEDS_PATCH`, assign stable finding IDs and exact bounded patches/tests. For `PASS_WITH_RISK`, identify the new
   material risk requiring Leo/GPT acceptance; D-009-A's already accepted limited uncertainty is not a new decision.
9. Stage only the two Reviewer result paths, inspect the cached diff, make one report-only commit, push `master`, verify
   the actual origin SHA and clean worktree, return only the canonical `REVIEW_RESULT_WRITTEN` pointer block to Advisor,
   and STOP.
