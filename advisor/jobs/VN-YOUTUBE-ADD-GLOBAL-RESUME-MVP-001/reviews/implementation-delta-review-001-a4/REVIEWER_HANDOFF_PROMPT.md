# Reviewer Handoff — implementation-delta-review-001-a4

1. Verify exact repo/origin/`master`/origin/clean state, D-011 ACK, prior/new subjects, frozen design, a3 report,
   rework-4 content/pointer, ancestry, full 87-path target, and exact two-path product delta.
2. Open `REVIEWER_BRIEF.md` and every named current artifact directly. Reload the canonical Reviewer/run/result rules,
   but do not repeat broad completed discovery or execute from summaries.
3. Perform only the same fixed Reviewer's `IMPLEMENTATION_DELTA_REVIEW` for `IR-F1-D1(g)-L`. Inspect actual normal
   runtime code and run the relevant/full deterministic tests. Prove whether any existing-binding failure can create or
   replace a key or binding, call downstream acceptance, or emit success evidence.
4. Specifically decide production-default owner enforcement, empty-state provisioning, valid-key idempotence,
   missing/invalid/symlink/wrong-owner/wrong-mode/wrong-size fail-closed behavior, complete row immutability, and all
   earlier closures/regressions.
5. No live call, secret access/output, runtime mutation, deployment, device run, patch, design change, risk acceptance,
   scope widening, substitute actor, agent, or subagent.
6. Write only:
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-delta-review-001-a4/REVIEW_RESULT.md`
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-delta-review-001-a4/REVIEW_RESULT_POINTER.md`
7. Use every canonical Reviewer field; target `SUBJECT_HEAD` and the 87 paths; set `PREVIOUS_SUBJECT_HEAD`; use
   `REPORT_HEAD: RECORDED_AFTER_REPORT_PUSH_IN_CHAT` in both containing files. Stage only the two Reviewer paths, make
   one report-only commit, push, verify origin equality and clean state, return only the canonical pointer to Advisor,
   and STOP.

An a4 `PASS` opens the already-approved real private acceptance. Any `NEEDS_PATCH`, `FAIL`, or `PASS_WITH_RISK` returns
the exact finding to Leo/GPT; do not infer another correction.
