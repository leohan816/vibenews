# Reviewer Handoff — design-delta-review-001

1. Verify the exact repository, origin, `master`, immutable subject heads, ancestry, `origin/master`, and current
   staged/unstaged/untracked state.
2. Open `CLAUDE.md`, `AGENTS.md`, `docs/agent/ROLE_INDEX.md`, every canonical protocol it requires, this handoff, and
   `REVIEWER_BRIEF.md` directly. Then open every job, review, decision, and immutable subject named by the brief.
3. Read the exact path-filtered delta from `PREVIOUS_SUBJECT_HEAD` to `SUBJECT_HEAD`; verify the eight changed subject
   paths, six unchanged blobs, every expected interleaved evidence path, and the separate Designer pointer commit.
4. Use the `agent-reach` skill only as needed for current official DeepSeek/Fish policy/API facts, with official
   provider sources. Do not use third-party policy claims or perform live provider calls.
5. Perform only the same fixed VibeNews Reviewer's independent `DESIGN_DELTA_REVIEW` for `DR1-F1`. Verify closure and
   retained whole-design requirements. Do not edit, patch, implement, freeze, or route another actor.
6. Never open or print `.env.server.local` values. Do not retrieve or retain caption text, provider bodies, configured
   values, private audio, original media, credentials, or secrets.
7. Write only:
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/design-delta-review-001/REVIEW_RESULT.md`
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/design-delta-review-001/REVIEW_RESULT_POINTER.md`
8. Use the canonical Reviewer fields, including `REVIEW_TYPE`, `REVIEW_ID`, `REVIEW_PASS`, `DESIGN_ID`,
   `DESIGN_CONTENT_HEAD`, all 14 `DESIGN_SUBJECT_PATHS`, `PREVIOUS_SUBJECT_HEAD`, `FINDING_IDS_IN_SCOPE`, and exact
   expected interleaved evidence paths. Use `FROZEN_DESIGN_HEAD: NOT_APPLICABLE` and
   `REPORT_HEAD: RECORDED_AFTER_REPORT_PUSH_IN_CHAT` inside both files.
9. Stage only the two Reviewer result paths, inspect the cached diff, create one report-only commit, push `master`,
   verify its actual SHA on `origin/master`, return only the concise `REVIEW_RESULT_WRITTEN` pointer block to Advisor,
   and STOP. Do not launch another agent, subagent, session, Worker, or Designer.
