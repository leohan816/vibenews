# Reviewer Handoff — implementation-delta-review-001-a3

1. Verify exact repo/origin/`master`/origin/clean state, D-010 ACK, prior/new subject objects, frozen design, all earlier
   implementation-review reports, exceptional rework content/pointer, ancestry, full 87-path target, and exact two-path
   product delta.
2. Open `REVIEWER_BRIEF.md`, both root entries, every canonical protocol, all named immutable artifacts, both changed
   subject files, previous blobs, and relevant called code directly. Do not execute from memory or summaries.
3. Perform only the same-Reviewer exceptional `IMPLEMENTATION_DELTA_REVIEW` for `IR-F1-D1(b)`/`(g)`. Resolve every
   risk-focused question with direct code/Git/test evidence, including the frozen existing-binding/missing-key lifecycle
   and the real normal-runtime access probes. Run full synthetic checks; no live calls, secrets, runtime mutation, or
   device run.
4. Do not patch, implement, freeze, accept risk, broaden scope, route another actor, or launch an agent/subagent.
5. Write only:
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-delta-review-001-a3/REVIEW_RESULT.md`
   - `runs/reviewer/VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001/implementation-delta-review-001-a3/REVIEW_RESULT_POINTER.md`
6. Use every canonical Reviewer field; target `SUBJECT_HEAD` and 87 paths; set `PREVIOUS_SUBJECT_HEAD`; scope findings
   to `(b)`/`(g)`, delta regressions, or directly implicated frozen behavior; and use
   `REPORT_HEAD: RECORDED_AFTER_REPORT_PUSH_IN_CHAT` in both containing files.
7. Stage only those two Reviewer paths, make one report-only commit, push, verify actual origin SHA and clean worktree,
   return only the canonical pointer block to Advisor, and STOP. Any non-pass returns to Advisor/Leo-GPT; no attempt 4.
