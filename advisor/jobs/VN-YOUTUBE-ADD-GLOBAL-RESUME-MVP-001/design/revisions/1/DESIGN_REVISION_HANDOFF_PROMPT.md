# Design Revision Handoff — design-revision-001

1. Verify the exact repo, origin, `master`, immutable input head from the launcher, `origin/master`, clean state, and
   ancestry from the previous design and review report.
2. Open root entries, all canonical protocols, `DESIGN_REVISION_BRIEF.md`, every required Advisor decision/review
   artifact, and every immutable subject/evidence file named there directly.
3. Perform only the same VibeNews Designer's bounded revision attempt 1 for `DR1-F1` and D-009-A. Do not redesign,
   implement, self-review, freeze, or route another actor.
4. Use the `agent-reach` search/web routing skill for current official provider policy facts; use only official
   DeepSeek and Fish Audio pages. Do not retain long quotes or third-party assertions.
5. Never open `.env.server.local` or output/copy any secret, configured value, reference voice, provider body,
   caption text, private audio, or private data.
6. Change only the exact authorized delta paths. Preserve every unrelated D-001 through D-008, provider, cap,
   pipeline, playback, privacy, retention, acceptance, and implementation-boundary requirement.
7. Write the complete version-2 Designer result and reconciled delta, then validate all 14 subject objects and finding
   closure. Stage/commit/push only authorized content paths.
8. In a second commit update only `DESIGN_RESULT_POINTER.md`, push, verify origin and clean state, and return only the
   concise version-2 pointer block to Advisor.
9. Do not launch/message Reviewer or Worker. Stop after return.
