# Designer Handoff — VN-YOUTUBE-ADD-GLOBAL-RESUME-MVP-001

Perform only the VibeNews Designer role in `DESIGNER_BRIEF.md`.

1. Verify `/home/leo/Project/VibeNews`, expected origin, `master`, clean staged/unstaged/untracked state, and the exact
   immutable `INPUT_HEAD` supplied in the short launcher against local `HEAD` and `origin/master`.
2. Open both root entries, every canonical protocol, every job artifact and read-only/source/product file required by
   the brief, and the exact Expo SDK 57 official documentation needed for proposed Expo contracts.
3. Treat `01_ADVISOR_BRIEF.md` as the current D-001 through D-008 ACK. Do not reopen resolved decisions. Stop only on
   an actual mismatch, authority conflict, or new material decision and return it solely to Advisor.
4. Do not open `.env.server.local`, do not inspect or output any secret/model/reference value, and never place secret,
   transcript, copyrighted source text, provider response, or private raw data in Git, logs, result files, or chat.
5. Author design only. Write the complete `DESIGN_RESULT.md` and reconcile only the exact authorized product-design
   paths. Do not write product code, tests, packages, config, database, server/runtime, deployment, canonical
   governance, Advisor artifacts, Worker artifacts, or Reviewer artifacts.
6. Select one low-risk official technology video with public captions and one official technology channel within
   D-008. Record only stable public identifiers/URLs needed for later private acceptance, never transcript content.
7. Preserve the corrected global-resume contract and explicitly eliminate stale five-second exclusion, alternate
   LLM, public deployment, mock-only, and sample-audio-only interpretations.
8. Stage only the exact changed design subject paths, inspect the cached diff, commit the content, push `master`, and
   verify origin. The content commit includes `DESIGN_RESULT.md` plus authorized product-design deltas and uses the
   required content-head sentinel.
9. Create/update only `DESIGN_RESULT_POINTER.md` with the actual content head and pointer-head sentinel, stage only
   that file, commit the pointer-only commit, push, and verify origin.
10. Return only the concise Designer pointer block with actual content and pointer heads to Advisor, then stop. Do
    not launch or message Reviewer or Worker and do not freeze or approve your own design.
