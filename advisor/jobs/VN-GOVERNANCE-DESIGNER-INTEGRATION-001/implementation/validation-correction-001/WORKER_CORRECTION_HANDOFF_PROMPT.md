# Worker Validation Correction Handoff — validation-correction-001

1. Open `WORKER_CORRECTION_BRIEF.md` directly and verify its launcher-supplied `CORRECTION_INPUT_HEAD` equals local
   HEAD and `origin/master`; require a clean staged/unstaged/untracked state.
2. Open both entry files, every canonical protocol, the frozen design, design-review PASS report, Advisor validation,
   previous Worker result/pointer, and the two current patch target files directly.
3. Perform only the three named corrections in the two exact canonical paths. Do not broaden wording or touch any
   other subject, design, report, Advisor, template, runtime, product, or repository path.
4. Directly reread the corrected files and execute every anchored-schema, authority, path, integrity, immutability,
   runtime-zero, dirty-state, and origin check in the brief.
5. Write only the declared `REWORK_RESULT.md` with the correction content commit and the separate pointer-only
   `REWORK_RESULT_POINTER.md` commit. Do not amend or rewrite prior Worker commits.
6. Return only the concise correction pointer block with actual content and pointer heads to Advisor and STOP. Do not
   launch or message Designer, Reviewer, Leo/GPT, another agent, or another session.
