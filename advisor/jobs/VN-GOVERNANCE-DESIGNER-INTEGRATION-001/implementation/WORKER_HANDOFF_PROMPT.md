# Worker Handoff — VN-GOVERNANCE-DESIGNER-INTEGRATION-001

1. Open `WORKER_BRIEF.md` directly, then perform every preflight and required direct read in that file.
2. Require the actual launcher `WORKER_INPUT_HEAD` to equal local HEAD and `origin/master` before writing. Require a
   clean worktree and the exact frozen design and `PASS` review objects to exist.
3. Perform only the VibeNews Worker role. Implement the frozen design only in the exact subject allowlist and write
   only the two Worker-owned result paths. Do not edit design, Reviewer, Advisor, runtime, product, or forbidden files.
4. If implementation exposes a contradictory, incomplete, unsafe, or materially undecided design, stop without
   guessing and return a file-first `BLOCKED_DESIGN_DEFECT` only to Advisor. No checkpoint is authorized.
5. Directly read every new/changed Markdown file and run every required integrity, reachability, stale-contract,
   ownership, path, ancestry, dirty-state, and origin check.
6. Create and push the Worker content/result commit, then the separate pointer-only commit exactly as specified. Do
   not amend, merge, rewrite, switch/create branches, or broaden scope.
7. Return only the concise Worker pointer block to Advisor with actual content and pointer heads, then STOP. Do not
   launch or message Designer, Reviewer, Leo/GPT, another agent, or another session.
