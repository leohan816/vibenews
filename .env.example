# VibeNews 환경변수 예시. 이 파일을 복사해 `.env` 로 만들고 값을 채운다.
#   cp .env.example .env
# 실제 값이 든 .env 는 절대 커밋하지 않는다(.gitignore가 막고 있음).
# 클라이언트에 노출할 값만 EXPO_PUBLIC_ 접두사를 붙인다. 비밀 키는 서버에서만 사용한다.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
