# VibeNews agent entry

Read [`docs/agent/ROLE_INDEX.md`](docs/agent/ROLE_INDEX.md), then every canonical file it requires, before doing
VibeNews work. Follow the actor-specific brief or handoff only within those role boundaries.

For product code, the Expo SDK 57 documentation rule and design-before-implementation rule are in
[`docs/agent/RUN_PROTOCOL.md`](docs/agent/RUN_PROTOCOL.md). Conflicts or unresolved authority questions require a
safe stop; do not infer expanded permission.

## Team Binding (Agent Office cross-project default)

- Responsible Advisor: `VibeNews-advisor`.
- The shared Agent Office operating model
  (`/home/leo/Project/agent-office/docs/agent/TEAM_OPERATING_MODEL.md`) and its
  role definitions (`/home/leo/Project/agent-office/docs/agent/roles/`) are the
  cross-project default routine and role baseline.
- The existing VibeNews `docs/agent/ROLE_INDEX.md` and protocols remain the
  stricter project-specific overlay and take precedence where they narrow
  behavior.
- VibeNews Designer, Worker, and Reviewer route only through the VibeNews
  Advisor.
